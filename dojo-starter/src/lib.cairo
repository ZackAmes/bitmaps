mod token {
    mod erc721;
    mod interface;
    mod models;
}

mod systems {
    mod actions;
}

mod models {
    mod moves;
    mod position;
}

mod tests {
    mod constants;
    mod utils;
    mod test_world;
}
